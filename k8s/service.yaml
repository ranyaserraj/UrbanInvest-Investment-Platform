apiVersion: v1
kind: Service
metadata:
  name: urbaninvest-service
  labels:
    app: urbaninvest-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: urbaninvest-app
